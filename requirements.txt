# Core dependencies
pydantic==1.10.*
python-dotenv==1.0.*
requests==2.32.*
urllib3==2.2.*

# API dependencies
fastapi==0.104.*
uvicorn[standard]==0.35.*
websockets==15.0.1

# MT5 dependencies
# mt5linux==0.1.*  # Only available when running in Docker/Wine environment
# Note: MetaTrader5 package is installed in Wine environment

# Testing dependencies (temporary - remove after PR merge)
pytest==7.4.*
pytest-cov==4.1.*
pytest-asyncio==0.21.*
httpx==0.25.*